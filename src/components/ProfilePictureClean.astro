---
const { class: className = '', ...rest } = Astro.props;
---

<img
    {...rest}
    class={`rounded-full transition-transform duration-300 hover:scale-110 ${className}`}
    src="natalia-varela.png"
    alt="natalia varela profile picture"
/>
