---
const { class: className = '', ...rest } = Astro.props;
---

<img
    {...rest}
    class={`rounded-3xl transition-transform duration-300 hover:scale-110 ${className}`}
    src="natalia-varela.jpg"
    alt="natalia varela profile picture"
/>
