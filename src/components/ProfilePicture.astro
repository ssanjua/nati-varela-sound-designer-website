---
const { class: className = '', ...rest } = Astro.props;
---

<img
    {...rest}
    class={`transition-transform duration-300 hover:scale-110 ${className}`}
    src="hero.webp"
    alt="paula pallares profile picture"
/>
